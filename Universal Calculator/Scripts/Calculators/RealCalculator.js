class RealCalculator {
  add(a, b)      { return a + b; }
  sub(a, b)      { return a - b; }
  multiply(a, b) { return a * b; }
  divide(a, b)   { return a / b; }
  pow(a, n)      { return Math.pow(a, n); }
  prod(a, p)     { return a * p; }

  one()  { return 1; }
  zero() { return 0; }

}